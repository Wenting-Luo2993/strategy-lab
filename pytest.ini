[pytest]
minversion = 8.0
addopts = -ra --strict-markers --disable-warnings
pythonpath = .
testpaths = vibe tests
asyncio_mode = auto
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    performance: marks performance regression tests
